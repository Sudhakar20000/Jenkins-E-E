name: feature
on:
  push:
  workflow_dispach:
jobs:
  feature-workflow:
    if: github.ref_name != 'main'
    uses: sudhakar20000/Workflows/.github/workflows/nodejs-eks-feature.yaml@main
    with:
      project: roboshop
      component: catalogue

  main-workflow:
    runs-on:  ubuntu-latest
    steps:
      - name: main workflow
        run: echo "this runs when a commit comes to main branch"